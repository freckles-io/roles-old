---
# defaults file for install-freckles

use_conda: true   # otherwise use pip and sudo to install dependencies
environment_name: "inaugurate"
pip_base_dir: "{{ ansible_env.HOME }}/.local/inaugurate/virtualenvs/{{ environment_name }}"
update: true
